from django.apps import AppConfig


class DrivingInstructorsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.driving_instructors"
    verbose_name = "Автоинструкторы"
